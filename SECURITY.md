# Security

- 本地 API 只监听 `127.0.0.1`，不会绑定局域网或公网地址。
- 秘密写入系统钥匙串；源码、SQLite 审计库和前端快照都不保存明文凭据。
- 本地 SQLite 只保存已准备的模拟订单历史；不保存第三方消息内容、会话或机器人凭据。
- 当前版本不接收交易所 API Key，也没有真实下单权限。
- 发布前执行仓库敏感词扫描，并确保 `.env`、会话、数据库、构建缓存不在 Git 中。

若未来增加真实下单，必须默认禁用提款权限、限定 API IP、启用独立子账户，并为每笔订单保留人工确认和硬性风险上限。
